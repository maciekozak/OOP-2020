//
// Created by student on 2/4/21.
//

#include <boost/test/unit_test.hpp>
#include "GameLogic.h"

BOOST_AUTO_TEST_SUITE(GameLogicTestSuite)

    BOOST_AUTO_TEST_CASE(testLogic) {
    // it doesn't make sense to test it after some modification we did... to be honest,
    // only possible function to test would be isCollision(priv) or spawnEntity(pub)
    // but it is needed to replicate whole program, so we leave it empty after all
    //
    // *for further usage: please try to test the function here before adding to project
    }

BOOST_AUTO_TEST_SUITE_END()